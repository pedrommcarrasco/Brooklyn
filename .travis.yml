language: swift
osx_image: xcode12

install: true

env:
  global:
  - PROJECT=Brooklyn.xcodeproj
  - SCHEME=Brooklyn

script:
 - set -o pipefail
 - xcodebuild $PROJECT clean -scheme "$SCHEME"
 - xcodebuild $PROJECT build -scheme "$SCHEME"
